# 에버플래닛 프로토타입 기획 문서 (싱글플레이어 버전)

## 1. 개요

- **목적**: 원작 에버플래닛(넥슨 서비스 종료 후 러쉬에잇이 IP 개방 프로젝트로 재개발 중인 캐주얼 MMORPG)의 세계관과 핵심 컨셉을 참고해, 혼자서 만들어보는 개인 취미/학습용 싱글플레이어 프로토타입을 제작한다.
- **목표 범위**: "행성 이동 구조 + 성장 시스템"이라는 원작의 핵심 아이덴티티를 작게 재현하는 것. MMO 요소(서버, 다중접속, 실시간 채팅/파티)는 전부 제외하고, 오프라인 싱글플레이어로 축소한다.
- **차후 계획**: 프로토타입이 잘 나오면 포트폴리오용으로 다듬어 이력서/포트폴리오에 활용.

## 2. 세계관 (원작 요소 재해석)

- **하네아 은하계**: 게임의 배경이 되는 은하계. 여러 행성으로 구성.
- **마이 플래닛 (My Planet)**: 플레이어 전용 개인 공간이자 허브. 여기서 캐릭터를 꾸미고, 성장 현황을 확인하고, 다른 행성으로 이동한다.
- **아스마라 / 이타카 / 노바루나**: 탐험 가능한 3개의 행성. 각각 테마와 난이도가 다르다.
  - 아스마라: 초반 행성. 평화로운 초원/숲 테마, 저레벨 몬스터.
  - 이타카: 중반 행성. 사막/유적 테마, 중간 난이도.
  - 노바루나: 후반 행성. 신비로운 우주/크리스탈 테마, 고난도. NPC '가위브러쉬' 등장 (퀘스트/상점 NPC로 재해석).
- **행성 이동 구조**: 마이 플래닛에서 포탈(또는 성계 지도 UI)을 통해 각 행성으로 이동. 각 행성은 별도의 씬(Scene)으로 구현하며, 특정 레벨 이상이어야 입장 가능하도록 게이트를 둔다.

## 3. 핵심 루프 (Core Loop)

1. 마이 플래닛(허브)에서 시작, 현재 레벨/스탯 확인
2. 이동 가능한 행성 중 하나를 선택해 이동
3. 행성에서 몬스터를 처치하며 경험치 획득
4. 레벨업 → 스탯 상승, 일정 레벨마다 스킬 해금
5. 목표 레벨 도달 시 다음 행성 게이트 오픈
6. 다시 마이 플래닛으로 귀환 가능 (반복)

## 4. 시스템 설계

### 4.1 플레이어
- 이동: WASD/방향키, 탑다운 or 3인칭 시점 (프로토타입은 탑다운 2D/2.5D 기준으로 설계, 3D로도 이식 쉬움)
- 전투: 근접 공격(쿨다운 기반) + 향후 확장 가능한 스킬 슬롯 1개
- 체력(HP)이 0이 되면 마지막 안전지점(마이 플래닛)으로 리스폰

### 4.2 성장 시스템
- 레벨, 경험치(XP), 공격력(ATK), 방어력(DEF), 최대체력(MaxHP)
- 레벨업 곡선: 다음 레벨 필요 경험치 = 기본값 * (레벨 ^ 1.5) 형태의 간단한 지수 곡선
- 레벨업 시 스탯 자동 상승 + 이벤트 발행(UI 갱신, 이펙트 훅 포인트 제공)

### 4.3 행성/이동 시스템
- 각 행성 = 별도의 Unity 씬
- 행성별 진입 최소 레벨 설정 (아스마라: 1, 이타카: 5, 노바루나: 10 — 프로토타입 기본값, 추후 조정 가능)
- 마이 플래닛의 포탈 오브젝트에 목적지 씬 이름 + 요구 레벨을 지정해두고, 상호작용 시 레벨 체크 후 씬 로드
- 플레이어의 레벨/스탯 데이터는 씬을 넘어가도 유지되어야 하므로 싱글턴 GameManager가 보관

### 4.4 전투/적 AI
- 상태 머신: Idle → Patrol → Chase → Attack → (사망 시 Die)
- 플레이어가 감지 범위에 들어오면 추적, 공격 범위에 들어오면 공격
- 처치 시 지정된 경험치를 플레이어에게 지급하고 오브젝트 제거

### 4.5 UI
- HUD: 체력바, 레벨, 경험치바, 현재 행성 이름
- 행성 이동 시 로딩/전환 안내 텍스트
- (선택) 레벨업 시 화면에 간단한 안내 팝업

### 4.6 저장/불러오기
- 프로토타입 범위에서는 PlayerPrefs 기반의 간단한 저장(레벨, 경험치, 최고 도달 행성)만 구현. 추후 확장 시 JSON 파일 저장으로 교체 가능하도록 인터페이스 분리.

## 5. 프로토타입에서 제외하는 것 (Out of Scope)

- 멀티플레이/서버 통신, 파티플레이, 채팅
- 아이템/인벤토리, 장비 시스템 (추후 확장 포인트로만 코드에 훅 남김)
- 퀘스트 시스템 (추후 확장)
- 사운드/이펙트, 정식 아트 리소스 (프로토타입은 기본 도형/placeholder 스프라이트로 대체)

## 6. 코드 구조

```
Assets/Scripts/
  Core/         - GameManager(싱글턴), SaveSystem
  Player/       - PlayerController, PlayerStats
  Planet/       - PlanetPortal, SpawnPoint, SceneSpawnHandler
  Combat/       - EnemyAI, EnemyStats, IDamageable, CombatUtility
  UI/           - HUDController, LevelUpPopup
  Data/         - PlanetDefinition(ScriptableObject), EnemyDefinition(ScriptableObject)
  CameraSystem/ - CameraFollow
  Environment/  - PlayerBoundary
```

이 구조는 이후 실제 Unity 프로젝트에 그대로 임포트해서 사용할 수 있도록 설계되었다.

## 7. 구현 단계 (실제 진행 순서)

기획 범위 전체를 한 번에 만들지 않고, 아래처럼 단계를 나눠서 하나씩 완성해가는 방식으로 진행한다.

1. **1단계 (현재 진행 중) - 첫 행성 탐험**: 전투/성장/행성 이동 전부 제외하고, 캐릭터 이동 + 카메라 추적 +
   맵 경계/장애물만으로 "첫 행성(아스마라)을 돌아다니는" 느낌을 먼저 완성한다.
2. **2단계 - 전투**: 1단계 씬에 몬스터를 배치하고 전투(공격/피격/처치) 붙이기
3. **3단계 - 성장 시스템 연동**: GameManager/PlayerStats 연결, 레벨업에 따른 스탯 변화 확인
4. **4단계 - 행성 이동**: 마이 플래닛 + 포탈 + 씬 전환 붙이기, 이타카/노바루나 확장
5. **5단계 - 저장/UI 마감**: HUD, 레벨업 팝업, 저장/불러오기 다듬기

이렇게 나누면 각 단계마다 실제로 플레이해볼 수 있는 결과물이 나오기 때문에, 중간에 재미가 없거나
방향을 바꾸고 싶어지는 시점을 빨리 파악할 수 있다는 장점이 있다.
